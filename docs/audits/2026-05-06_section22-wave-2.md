# Tier 5 — §22 Math Regression Wave 2
**Date:** 2026-05-06
**Worktree:** `.claude/worktrees/exciting-lovelace-60ae5b`
**Predecessor:** `2026-05-05_section22-regression.md` (Wave 1 — 6/6 PASS, 81/0 ancillary)

## Regression status

| Suite | Result |
|---|---|
| `test_composite_signal_regression.py` | **PASS — 6/6** (baseline 6/6) |
| `test_composite_critical_fixes.py` | PASS — 5/5 |
| `test_composite_fallback.py` | PASS — 6/6 |
| `test_composite_learned_weights.py` | PASS — 10/10 |
| `test_composite_weight_optimizer.py` | PASS — 9/9 |
| **Ancillary math total** | **PASS — 36/36** (incl. regression suite) |
| `test_pct_change_p0_3.py` | PASS — 8/8 |
| `test_diagnostics_feeds_p0_10.py` | PASS — 6/6 |
| **New P0 total** | **PASS — 14/14** |

Run wall time: 4.0s (regression), 5.0s (math suite), 6.7s (P0). Zero failures, zero errors. One deprecation warning (langchain-core / Pydantic V1 on Python 3.14) — unrelated to math.

§22 baseline scenarios all green: `all_none_neutral_baseline`, `extreme_risk_on_bull`, `extreme_risk_off_bear`, `mid_cycle_balanced`, `panic_vix_gate_check`, `test_baseline_file_present`.

## P0-3 change safety analysis

**Scope of change:** Single commit `3f8b934` ("fix(P0-3): wire price + 24h/30d/1y deltas into engine scan-result dict") modifying `crypto_model_core.py` only. `composite_signal.py` last touched in commit `61b1c7c` (Phase 1 hotfix, pre-2026-05-04 baseline) — confirmed unchanged since the §22 baseline was locked.

**Code added (`crypto_model_core.py` lines 4794-4854):**

1. **Inner helper `_pct_change_from_frame(_df, _bars_back)`** (lines 4806-4816) — pure function, reads `_df['close'].iloc[-1]` and `_df['close'].iloc[-1 - _bars_back]`, returns `None` on short frames or zero-divide. No mutation, no side effects.

2. **Frame lookups** (lines 4818-4821) — `_ohlcv_frames.get('1h'|'4h'|'1d'|'1w')` is **read-only `.get()`**. Cross-checked all `_ohlcv_frames` references in the file: only writes are at lines 4425-4431 (the original pre-scan fetch loop, well before signal math). All other references including the new P0-3 lines are reads. **No mutation introduced.**

3. **Delta computation** (lines 4823-4831) — three independent percent-change reads with documented fallback chains. Pure read math; cannot affect any prior variable.

4. **`current_price` fallback walk** (lines 4835-4844) — only fires when `current_price` is falsy. Reads `_fallback_frame['close'].iloc[-1]`, assigns to local `current_price` variable. This local was already used downstream for `_price_rounded`; the fallback ONLY replaces a missing/zero price with a valid last-close, never overrides a good price. **Cannot affect signal math** because the local `current_price` is not consumed by any prior signal computation — by line 4835, all of `direction_avg`, `effective_direction`, `conf_avg`, Wyckoff, Pi-cycle have already been computed and frozen.

5. **Result dict** (lines 4848-4896) — new keys `price`, `change_24h_pct`, `change_30d_pct`, `change_1y_pct` are **additions**. `price_usd` is **preserved** (line 4850) for back-compat. No existing key was renamed, removed, or had its value source changed. New keys do not collide with any pre-existing key in the dict.

**Ordering proof (math-safe):**
- Line 4763: `effective_direction = direction_avg` (computed)
- Line 4775-4777: `conf_avg` finalized via Wyckoff bias
- Line 4787-4792: Pi-cycle suppressor applies — final `conf_avg` and `effective_direction`
- Line 4794+: P0-3 code begins → operates only on `_ohlcv_frames` read state and `current_price` local

**Verdict: math-safe.** P0-3 is a pure read-and-attach pattern post-signal-finalization. The 6/6 regression PASS empirically confirms zero drift; the code structure analytically guarantees it.

## CMC-1/2/3 status

Read source: `docs/audits/2026-05-04_section22-math-findings-deferred.md`.

| Finding | Touched since 2026-05-04? | Deferral valid for V1 mobile launch? | Recommendation |
|---|---|---|---|
| **CMC-1** scalar broadcast in `composite_signal.py` | **No** — `composite_signal.py` last edited in commit `61b1c7c` (pre-baseline). 6/6 regression PASS. | **Yes.** Edge case is rare on Tier 1-3 (BTC/ETH-dominant universes); mobile V1 ships Tier 1-3 by default. Tier 5 (top-100) latent risk is pre-existing, not new. | **Defer to post-V1.** Schedule the half-day math sign-off batch after launch. |
| **CMC-2** `.shift(-1)` look-ahead | **No.** P5-LA-1 (closed-bar pivot policy) is locked, no revisit triggered. | **Yes.** Documented inconsistency, not an observable regression. Backtest baseline matches production. | **Defer.** Re-evaluate only if/when P5-LA-1 is reopened. |
| **CMC-3** chandelier exit comment-vs-math | **No.** `composite_signal.py` unchanged. No chandelier-exit change proposed. | **Yes.** Math is correct (industry-standard form); only the comment is alleged misleading. Zero behavioral risk. | **Defer.** Trivial doc fix that can be batched with future chandelier work — no V1 blocker. |

**Deferral rationale still valid for all three.** The hard constraint at the bottom of `2026-05-04_section22-math-findings-deferred.md` — *"Do not modify `composite_signal.py` to address these three findings during Phase D"* — remains in force. Nothing in today's changes (P0-3, the `_toFiniteNumber`/`_toCleanString` frontend HOTFIX, or the path-param encoding fix from `0e7ef9d`) touches signal math. The §22 baseline is intact.

**Trigger conditions** (from the deferred-findings doc) all hold negative:
- Regression suite below 6/6? **No** — still 6/6.
- Tier 5 single-layer drift observed? **No reports.**
- P5-LA-1 reopened? **No.**
- Chandelier-exit change proposed? **No.**

## Recommendation

**PASS — no action required.**

- §22 regression: 6/6 (zero drift vs. Wave 1).
- Ancillary math: 36/36 (zero new failures).
- New P0 suites: 14/14.
- P0-3 dict patch is provably math-safe — pure read-and-append after signal finalization, no `_ohlcv_frames` mutation, no key collisions.
- HOTFIX (`_toFiniteNumber` / `_toCleanString`) is frontend-only — not in scope for math regression.
- CMC-1/CMC-2/CMC-3 stay deferred to post-V1 mobile launch per 2026-05-04 cowork decision; their underlying file is unchanged and trigger conditions all negative.

**No D8 / V1 mobile launch blockers from a math regression standpoint.** Cleared to ship.

---
*Wave 2 sign-off — 2026-05-06. Next checkpoint: re-run after the next `composite_signal.py` edit, or pre-V1-mobile cutover, whichever first.*
